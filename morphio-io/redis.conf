# Morphio Redis Configuration
# This file is mounted in Docker containers

# Persistence
appendonly yes

# Memory management (optional - adjust for your environment)
# maxmemory 256mb
# maxmemory-policy allkeys-lru

# Connection
timeout 0
tcp-keepalive 300
